<% @user ||= current_user %>

<div class="stats">
	<%= link_to "#" do %>
		<strong class="stat">
			<%= @user.following.count %>
		</strong>
		following
	<% end %>

	<%= link_to "#" do %>
		<strong class="stat">
			<%= @user.followers.count %>
		</strong>
		followers
	<% end %>
</div>
